name: Update oc.software.18.04 and trigger oc.user.18.04

on:
  repository_dispatch:
    types: dispatch-from-oc.ubuntu.18.04
  push:
    branches:
      - main
jobs:
  build:
    # runs-on: self-hosted
    runs-on: ubuntu-latest
    steps:
      - name: Clone sources
        run: |
          rm -rf ..?* .[!.]* *
          git -c http.extraheader="Authorization: basic ${{ secrets.TOKEN }}" clone "https://github.com/abcdesktopio/oc.software.18.04.git" .
      
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v1

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1
 
      - name: Login to DockerHub
        uses: docker/login-action@v1 
        with:
         username: ${{ secrets.DOCKER_USERNAME }}
         password: ${{ secrets.DOCKER_TOKEN }}

      - name: Build and push oc.software.18.04
        uses: docker/build-push-action@v2
        with:
          context: .
          push: true
          build-args: |
            BASE_IMAGE_RELEASE=18.04
            BASE_IMAGE=abcdesktopio/oc.ubuntu.18.04:dev
          tags: |
            abcdesktopio/oc.software.18.04:dev
            
      - name: Build and push oc.software.20.04
        uses: docker/build-push-action@v2
        with:
          context: .
          push: true
          build-args: |
            BASE_IMAGE_RELEASE=20.04
            BASE_IMAGE=abcdesktopio/oc.ubuntu.20.04:dev
          tags: |
            abcdesktopio/oc.software.20.04:dev
            
      - name: Build and push oc.software.21.04
        uses: docker/build-push-action@v2
        with:
          context: .
          push: true
          build-args: |
            BASE_IMAGE_RELEASE=21.04
            BASE_IMAGE=abcdesktopio/oc.ubuntu.21.04:dev
          tags: |
            abcdesktopio/oc.software.21.04:dev      
            

      - name: Dispatch on repository oc.user.18.04
        uses: peter-evans/repository-dispatch@v1
        with:
          token: ${{ secrets.TOKEN_DISPATCH }}
          repository: abcdesktopio/oc.user.18.04
          event-type: dispatch-from-oc.software.18.04

